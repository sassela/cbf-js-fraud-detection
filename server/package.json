{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.9.0",
    "dotenv": "^16.3.1"
  }
}
